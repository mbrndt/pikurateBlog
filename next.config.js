/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"www.90daykorean.com",
			"lh3.googleusercontent.com",
		],
	},
};

module.exports = nextConfig;
